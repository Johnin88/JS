// Email
let email = document.getElementById('email')

// Mensagem
let mensagem = document.querySelector('#mensagem')

// Formulário - retirar o evneto pre definido
let formulario = document.querySelectorAll('form')[0]

// Botão - adicionar o evneto de enviar o formulário
let btn = getElementById('btnEnviar')

//antres de mostrar uma mensagem no alert verifique se existe uma mensagem no campo mensagem

function verificarMensagem() {
    let texto = mensagem.value // String
}